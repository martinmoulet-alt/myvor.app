"use client";

import { useMemo,useState } from "react";
import { AlertTriangle,CheckCircle2,Download,FileText,Link2,Sparkles,Target } from "lucide-react";
import { saveProduction } from "@/lib/productions";
import styles from "./ImpactCorporate.module.css";

type Dossier={id:string;client:string;title:string;objective:string;context:string;status:string;created_at:string;sector?:string|null;activity?:string|null;strategic_issues?:string[];risks_to_avoid?:string[];opportunities?:string[];client_position?:string|null;key_actors?:string[];watch_topics?:string[];watch_subtopics?:string[];reference_texts?:string[];key_deadlines?:string[];internal_notes?:string|null};
type Watch={id:string;title:string;nature:string;source_url:string;dossier_id:string|null;urgency:string;created_at:string};
type Evidence={kind:"official"|"dossier";source_title:string;source_url:string;excerpt:string;confidence:number};
type ImpactDisposition={disposition:string;impact_client:string;niveau:string;evidence?:Evidence|null};
type ScoreDetail={juridique?:number;economique_operationnel?:number;urgence?:number;probabilite?:number;politique_reputation?:number;capacite_action?:number};
type ScoreJustifications={juridique?:string;economique_operationnel?:string;urgence?:string;probabilite?:string;politique_reputation?:string;capacite_action?:string};
type ScoreEvidence=Partial<Record<keyof ScoreDetail,Evidence|null>>;
type SourceTraceStatus="fetched"|"unavailable"|"unsupported"|"not_requested"|"missing_url";
type SourceUsed={title:string;url:string;status?:SourceTraceStatus;read_chars?:number};
type Note={title?:string;executive_summary?:string;score?:number;level?:string;rationale?:string;risks?:string[];opportunities?:string[];deadlines?:string[];recommendations?:string[];sources_used?:SourceUsed[];dispositions_concernees?:ImpactDisposition[];informations_a_confirmer?:string[];score_detail?:ScoreDetail|null;score_justifications?:ScoreJustifications|null;score_evidence?:ScoreEvidence};
type ActionDraft={dossier_id:string;type:string;title:string;description?:string;actor_name?:string;priority:string;due_date?:string|null};
type ImpactDepth="express"|"standard"|"deep";

const depthOptions:{value:ImpactDepth;label:string;description:string}[]=[{value:"express",label:"Express",description:"Décision immédiate : score, alertes critiques et actions prioritaires."},{value:"standard",label:"Standard",description:"Note de travail complète pour le suivi quotidien du dossier."},{value:"deep",label:"Approfondie",description:"Analyse stratégique détaillée avec justification de chaque critère de notation."}];
const scoreLabels:{key:keyof ScoreDetail;label:string;max:number}[]=[{key:"juridique",label:"Juridique et réglementaire",max:20},{key:"economique_operationnel",label:"Économique et opérationnel",max:20},{key:"urgence",label:"Urgence institutionnelle",max:15},{key:"probabilite",label:"Probabilité d’évolution ou d’adoption",max:15},{key:"politique_reputation",label:"Politique et réputation",max:15},{key:"capacite_action",label:"Capacité d’action du client",max:15}];

function sourceTraceMeta(status?:SourceTraceStatus){if(status==="fetched")return{label:"Contenu lu par Myvor",color:"#16714e"};if(status==="unavailable")return{label:"Source inaccessible lors de l’analyse",color:"#b8263a"};if(status==="unsupported")return{label:"Contenu non lu — format non pris en charge",color:"#a85f00"};if(status==="not_requested")return{label:"Référencée mais non lue dans cette analyse",color:"#526278"};if(status==="missing_url")return{label:"Référencée sans URL exploitable",color:"#7a89a1"};return{label:"Statut de lecture non disponible",color:"#7a89a1"};}
function escapeHtml(value:unknown){return String(value??"").replace(/[&<>'"]/g,char=>({"&":"&amp;","<":"&lt;",">":"&gt;","'":"&#39;",'"':"&quot;"}[char]||char));}
function listHtml(items?:string[]){if(!items?.length)return"<p class='muted'>Aucun élément identifié.</p>";return`<ul>${items.map(item=>`<li>${escapeHtml(item)}</li>`).join("")}</ul>`;}
function evidenceHtml(evidence?:Evidence|null){if(!evidence)return"<div class='proof missing'><b>Preuve précise non retrouvée</b><span>À vérifier manuellement dans les sources lues.</span></div>";const label=evidence.kind==="official"?"Preuve institutionnelle":"Mémoire stratégique du dossier";return`<div class='proof'><b>${escapeHtml(label)} — ${escapeHtml(evidence.source_title)}</b><q>${escapeHtml(evidence.excerpt)}</q>${evidence.source_url?`<small>${escapeHtml(evidence.source_url)}</small>`:""}</div>`;}

export default function ImpactModule({dossiers,watch,onActions}:{dossiers:Dossier[];watch:Watch[];onActions?:(drafts:ActionDraft[])=>Promise<void>|void}){
  const[dossierId,setDossierId]=useState(dossiers[0]?.id||"");const[selectedIds,setSelectedIds]=useState<string[]>([]);const[loading,setLoading]=useState(false);const[error,setError]=useState("");const[note,setNote]=useState<Note|null>(null);const[saveMessage,setSaveMessage]=useState("");const[depth,setDepth]=useState<ImpactDepth>("standard");
  const dossier=dossiers.find(d=>d.id===dossierId)||null;const related=useMemo(()=>watch.filter(w=>w.dossier_id===dossierId),[watch,dossierId]);const effectiveIds=selectedIds.length?selectedIds:related.map(w=>w.id);
  async function generate(){if(!dossier){setError("Sélectionne un dossier client.");return;}const items=related.filter(w=>effectiveIds.includes(w.id));if(!items.length){setError("Aucun élément de veille n’est rattaché à ce dossier.");return;}setLoading(true);setError("");setSaveMessage("");setNote(null);try{const response=await fetch("/api/impact",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({dossier,items,depth})});const raw=await response.text();let payload:any=null;try{payload=raw?JSON.parse(raw):null;}catch{if(response.status===504)throw new Error("La Note approfondie a dépassé le temps de réponse du serveur.");throw new Error(`Le serveur Myvor a renvoyé une réponse invalide (${response.status}).`);}if(!response.ok)throw new Error(payload?.error||`Génération impossible (${response.status})`);const nextNote=payload.note||null;setNote(nextNote);if(nextNote){const title=String(nextNote.title||`Note d’impact — ${dossier.title}`);const saved=await saveProduction({dossier_id:dossier.id,type:"impact",title,content:{note:nextNote,item_ids:items.map(i=>i.id),depth,grounding:payload?.grounding||null}});setSaveMessage(saved.error?`Note générée, mais non enregistrée : ${saved.error.message}`:`Note ${depthOptions.find(option=>option.value===depth)?.label.toLowerCase()} enregistrée dans l’historique du dossier.`);}if(nextNote&&onActions){const priority=String(nextNote.level||"moyen");const drafts:ActionDraft[]=[{dossier_id:dossier.id,type:"note_client",title:`Envoyer la note d’impact — ${dossier.title}`,description:String(nextNote.executive_summary||"Note d’impact prête à partager avec le client."),priority}];if(Array.isArray(nextNote.deadlines)&&nextNote.deadlines.length)drafts.push({dossier_id:dossier.id,type:"echeance",title:`Vérifier la prochaine échéance — ${dossier.title}`,description:String(nextNote.deadlines[0]),priority,due_date:null});await onActions(drafts);}}catch(err:any){setError(err?.message||"Génération impossible");}finally{setLoading(false);}}
  function toggle(id:string){const base=selectedIds.length?selectedIds:related.map(w=>w.id);setSelectedIds(base.includes(id)?base.filter(x=>x!==id):[...base,id]);}
  function exportPdf(){if(!note||!dossier)return;const mode=depthOptions.find(option=>option.value===depth)?.label||"Standard";const score=Math.round(Number(note.score)||0);const scoreDetail=note.score_detail&&depth!=="express"?`<section><h2>Décomposition, justification et preuves du score</h2><div class='metrics'>${scoreLabels.map(metric=>`<div class='metric'><div class='metric-top'><span>${escapeHtml(metric.label)}</span><strong>${Math.round(Number(note.score_detail?.[metric.key])||0)}<small>/${metric.max}</small></strong></div><p>${escapeHtml(note.score_justifications?.[metric.key]||"Justification non disponible.")}</p>${evidenceHtml(note.score_evidence?.[metric.key])}</div>`).join("")}</div></section>`:"";const provisions=note.dispositions_concernees?.length?`<section><h2>Dispositions concernées</h2>${note.dispositions_concernees.map(item=>`<article class='provision'><div><strong>${escapeHtml(item.disposition||"Disposition à préciser")}</strong><span class='pill'>${escapeHtml(String(item.niveau||"moyen").replaceAll("_"," "))}</span></div><p>${escapeHtml(item.impact_client||"Impact client à confirmer.")}</p>${evidenceHtml(item.evidence)}</article>`).join("")}</section>`:"";const sources=note.sources_used?.length?`<section><h2>Traçabilité des sources</h2>${note.sources_used.map(source=>{const meta=sourceTraceMeta(source.status);const readDetail=source.status==="fetched"&&source.read_chars?` · ${Math.round(source.read_chars).toLocaleString("fr-FR")} caractères lus`:"";return`<div class='source'><div class='source-copy'><span>${escapeHtml(source.title)}</span><b class='source-status'>${escapeHtml(meta.label+readDetail)}</b></div>${source.url?`<small>${escapeHtml(source.url)}</small>`:""}</div>`;}).join("")}</section>`:"";const confirmations=note.informations_a_confirmer?.length?`<section class='confirm'><h2>Informations à confirmer</h2>${listHtml(note.informations_a_confirmer)}</section>`:"";const detailSection=depth==="express"?`<div class='grid'><section><h2>Alertes prioritaires</h2>${listHtml(note.risks?.slice(0,3))}</section><section><h2>Actions à lancer</h2>${listHtml(note.recommendations?.slice(0,3))}</section></div>${note.deadlines?.[0]?`<section><h2>Prochaine fenêtre d’action</h2><p>${escapeHtml(note.deadlines[0])}</p></section>`:""}`:`${scoreDetail}${provisions}<div class='grid'><section><h2>${depth==="deep"?"Risques stratégiques":"Risques"}</h2>${listHtml(note.risks)}</section><section><h2>${depth==="deep"?"Opportunités stratégiques":"Opportunités"}</h2>${listHtml(note.opportunities)}</section><section><h2>${depth==="deep"?"Échéances et fenêtres d’action":"Échéances"}</h2>${listHtml(note.deadlines)}</section><section><h2>${depth==="deep"?"Plan d’action recommandé":"Recommandations"}</h2>${listHtml(note.recommendations)}</section></div>${confirmations}`;const html=`<!doctype html><html lang='fr'><head><meta charset='utf-8'><title>${escapeHtml(note.title||"Note d’impact Myvor")}</title><style>@page{size:A4;margin:16mm}*{box-sizing:border-box}body{font-family:-apple-system,BlinkMacSystemFont,'Segoe UI',Arial,sans-serif;color:#14213d;margin:0;font-size:11.5px;line-height:1.5}.brand{font-weight:900;font-size:17px;color:#0b57d0}.top{display:flex;justify-content:space-between;gap:20px;border-bottom:2px solid #0b57d0;padding-bottom:12px;margin-bottom:22px}.meta{text-align:right;color:#68758a;font-size:10px}.eyebrow{text-transform:uppercase;letter-spacing:.12em;color:#0b57d0;font-size:9px;font-weight:800}.hero{display:grid;grid-template-columns:1fr 110px;gap:18px;margin-bottom:18px}.hero h1{font-size:22px;line-height:1.2;margin:4px 0 8px}.hero p{color:#46556d}.score{background:#0b244a;color:white;border-radius:12px;padding:14px;height:max-content}.score span{font-size:9px;text-transform:uppercase}.score strong{display:block;font-size:34px;line-height:1.05;margin-top:6px}.score em{font-style:normal;font-size:10px;text-transform:uppercase}.rationale{background:#f4f7fb;border:1px solid #dce5f0;padding:12px;border-radius:10px;margin-top:10px;white-space:pre-line}section{border:1px solid #dfe7f2;border-radius:10px;padding:13px;margin:0 0 12px;break-inside:avoid}section h2{font-size:13px;margin:0 0 8px}.grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.grid section{margin:0}ul{margin:0;padding-left:18px}li{margin:0 0 5px}.provision{border-top:1px solid #e7edf5;padding:9px 0}.provision:first-of-type{border-top:0}.provision>div,.metric-top{display:flex;justify-content:space-between;gap:10px}.provision p,.metric p{margin:5px 0 0;color:#4e5d73}.pill{font-size:9px;background:#edf2f8;border-radius:999px;padding:3px 7px}.metrics{display:grid;grid-template-columns:1fr 1fr;gap:8px}.metric{border:1px solid #e1e8f2;border-radius:8px;padding:10px;break-inside:avoid}.metric span{color:#68758a;font-size:9px}.metric strong{font-size:18px}.metric small{font-size:9px;color:#68758a}.metric p{font-size:10px;line-height:1.5}.proof{margin-top:8px;border-left:3px solid #16714e;background:#f3faf6;padding:8px 9px;display:grid;gap:4px}.proof b{font-size:9px;color:#16714e}.proof q{font-size:9.5px;color:#405069}.proof small{font-size:8px;color:#708099;overflow-wrap:anywhere}.proof.missing{border-color:#d69a28;background:#fff9ec}.proof.missing b{color:#9a6500}.proof.missing span{font-size:9px;color:#6c5b35}.confirm{background:#fffaf0;border-color:#efd9a2}.source{display:flex;justify-content:space-between;gap:14px;border-top:1px solid #edf1f6;padding:7px 0}.source:first-of-type{border-top:0}.source-copy{display:grid;gap:2px}.source-status{font-size:9px;color:#68758a;font-weight:700}.source small{color:#68758a;overflow-wrap:anywhere}.muted{color:#7a8798}.footer{margin-top:20px;border-top:1px solid #dfe7f2;padding-top:9px;display:flex;justify-content:space-between;color:#8792a3;font-size:9px}@media print{body{-webkit-print-color-adjust:exact;print-color-adjust:exact}}</style></head><body><div class='top'><div><div class='brand'>MYVOR</div><div class='eyebrow'>Note d’impact ${escapeHtml(mode)}</div></div><div class='meta'><div>${escapeHtml(dossier.client)}</div><div>${escapeHtml(new Date().toLocaleDateString("fr-FR"))}</div></div></div><div class='hero'><div><div class='eyebrow'>${escapeHtml(mode)}</div><h1>${escapeHtml(note.title||`Note d’impact — ${dossier.title}`)}</h1><p>${escapeHtml(note.executive_summary||"")}</p>${depth!=="express"&&note.rationale?`<div class='rationale'><strong>Lecture générale du score</strong><div>${escapeHtml(note.rationale)}</div></div>`:""}</div><div class='score'><span>Score d’impact</span><strong>${score}<small>/100</small></strong><em>${escapeHtml(note.level||"moyen")}</em></div></div>${detailSection}${sources}<div class='footer'><span>Myvor — Anticipez l’impact.</span><span>Les preuves affichées sont retrouvées dans les contenus effectivement lus.</span></div></body></html>`;const printWindow=window.open("","_blank");if(!printWindow){setError("L’export PDF a été bloqué par le navigateur. Autorise les fenêtres surgissantes puis réessaie.");return;}printWindow.document.open();printWindow.document.write(html);printWindow.document.close();printWindow.opener=null;setTimeout(()=>{printWindow.focus();printWindow.print();},500);}
  const level=String(note?.level||"moyen");const levelKey=level.replaceAll(" ","-") as keyof typeof styles;const sourceCount=note?.sources_used?.length||0;const readSourceCount=note?.sources_used?.filter(source=>source.status==="fetched").length||0;
  return <div className={styles.page}><header className={styles.head}><div><div className={styles.kicker}>Analyse stratégique</div><h1>Note d’impact</h1><p>Mesurez l’effet d’un texte sur l’objectif du client et transformez l’analyse en décisions opérationnelles.</p></div></header><section className={styles.workflow}><div className={styles.stepCard}><div className={styles.stepTitle}><span>1</span><div><b>Dossier analysé</b><small>Sélectionnez le client et son objectif.</small></div></div><div className={styles.field}><label>Dossier client</label><select value={dossierId} onChange={e=>{setDossierId(e.target.value);setSelectedIds([]);setNote(null);setError("");setSaveMessage("");}}><option value="">Sélectionner un dossier</option>{dossiers.map(d=><option key={d.id} value={d.id}>{d.client} — {d.title}</option>)}</select></div>{dossier&&<div className={styles.objective}><Target size={17}/><div><b>Objectif du client</b><p>{dossier.objective}</p></div></div>}</div><div className={styles.stepCard}><div className={styles.stepTitle}><span>2</span><div><b>Éléments de veille</b><small>Choisissez les textes à intégrer au calcul.</small></div></div>{related.length?<div className={styles.watchList}>{related.map(item=><label key={item.id} className={styles.watchItem}><input type="checkbox" checked={effectiveIds.includes(item.id)} onChange={()=>toggle(item.id)}/><div className={styles.watchCopy}><div><span className={styles.nature}>{item.nature}</span><span className={`${styles.impact} ${styles[item.urgency.replaceAll(" ","-") as keyof typeof styles]||""}`}>{item.urgency}</span></div><b>{item.title}</b></div></label>)}</div>:<div className={styles.empty}><AlertTriangle size={28}/><b>Aucun texte rattaché</b><span>Rattachez d’abord un élément depuis la page Veille.</span></div>}</div></section><section className={styles.depthPanel}><div className={styles.depthHead}><div><b>Type de note</b><span>Choisissez le niveau d’analyse adapté au dossier.</span></div></div><div className={styles.depthGrid}>{depthOptions.map(option=><button type="button" key={option.value} className={`${styles.depthOption} ${depth===option.value?styles.depthActive:""}`} onClick={()=>{setDepth(option.value);setNote(null);setError("");setSaveMessage("");}}><strong>{option.label}</strong><span>{option.description}</span></button>)}</div></section><section className={styles.launch}><div><div className={styles.launchIcon}><Sparkles size={20}/></div><div><b>Analyse prête — {depthOptions.find(option=>option.value===depth)?.label}</b><span>{effectiveIds.length} texte(s) sélectionné(s) pour {dossier?.client||"ce dossier"}.</span></div></div><button onClick={generate} disabled={loading||!dossier||!related.length}>{loading?"Analyse en cours…":`Générer la note ${depthOptions.find(option=>option.value===depth)?.label.toLowerCase()}`}</button></section>{error&&<div className={styles.error}>{error}</div>}{saveMessage&&<div className={styles.error}>{saveMessage}</div>}{note&&<section className={styles.result}><div className={styles.resultToolbar}><div><span>Livrable client</span><b>Note prête à être partagée</b></div><button type="button" onClick={exportPdf}><Download size={17}/>Exporter en PDF</button></div><div className={styles.resultHero}><div className={styles.resultCopy}><div className={styles.eyebrow}>Note {depthOptions.find(option=>option.value===depth)?.label}</div><h2>{note.title||`Note d’impact — ${dossier?.title||"Dossier"}`}</h2><p>{note.executive_summary}</p>{depth!=="express"&&note.rationale&&<div className={styles.rationale}><b>Lecture générale du score</b><span>{note.rationale}</span></div>}</div><div className={styles.scoreCard}><span>Score d’impact</span><strong>{Math.round(Number(note.score)||0)}</strong><small>/100</small><div className={`${styles.level} ${styles[levelKey]||""}`}>{level}</div></div></div>{depth==="express"&&<ExpressResult note={note}/>} {depth==="standard"&&<><ScoreBreakdown detail={note.score_detail} justifications={note.score_justifications} evidence={note.score_evidence} compact/><Provisions items={note.dispositions_concernees}/><div className={styles.sectionGrid}><ImpactSection title="Risques" icon={<AlertTriangle size={18}/>} items={note.risks}/><ImpactSection title="Opportunités" icon={<CheckCircle2 size={18}/>} items={note.opportunities}/><ImpactSection title="Échéances" icon={<Target size={18}/>} items={note.deadlines}/><ImpactSection title="Recommandations" icon={<Sparkles size={18}/>} items={note.recommendations}/></div><Confirmations items={note.informations_a_confirmer}/></>}{depth==="deep"&&<><div className={styles.deepBanner}><Sparkles size={19}/><div><b>Analyse stratégique approfondie</b><span>Chaque composante du score est justifiée séparément et reliée à une preuve retrouvée dans les contenus analysés lorsque c’est possible.</span></div></div><ScoreBreakdown detail={note.score_detail} justifications={note.score_justifications} evidence={note.score_evidence}/><Provisions items={note.dispositions_concernees}/><div className={styles.sectionGrid}><ImpactSection title="Risques stratégiques" icon={<AlertTriangle size={18}/>} items={note.risks}/><ImpactSection title="Opportunités stratégiques" icon={<CheckCircle2 size={18}/>} items={note.opportunities}/><ImpactSection title="Échéances et fenêtres d’action" icon={<Target size={18}/>} items={note.deadlines}/><ImpactSection title="Plan d’action recommandé" icon={<Sparkles size={18}/>} items={note.recommendations}/></div><Confirmations items={note.informations_a_confirmer}/></>}{!!note.sources_used?.length&&<div className={styles.sources}><div className={styles.sourcesHead}><FileText size={18}/><div><h3>Traçabilité des sources</h3><span style={{display:"block",marginTop:3,fontSize:12,color:"#7a89a1"}}>{readSourceCount} contenu(s) lu(s) sur {sourceCount} source(s) référencée(s).</span></div></div>{note.sources_used.map((source,index)=>{const meta=sourceTraceMeta(source.status);return<div className={styles.sourceRow} key={`${source.url}-${index}`}><div style={{display:"grid",gap:4,minWidth:0}}><span>{source.title}</span><small style={{color:meta.color,fontWeight:800,lineHeight:1.35}}>{meta.label}{source.status==="fetched"&&source.read_chars?` · ${Math.round(source.read_chars).toLocaleString("fr-FR")} caractères lus`:""}</small></div>{source.url&&<a href={source.url} target="_blank" rel="noreferrer">Lire le texte original</a>}</div>})}</div>}</section>}</div>;
}

function EvidenceProof({evidence}:{evidence?:Evidence|null}){if(!evidence)return<div className={`${styles.evidenceProof} ${styles.evidenceMissing}`}><AlertTriangle size={14}/><div><b>Preuve précise non retrouvée</b><span>Myvor n’a pas trouvé de passage suffisamment proche dans les contenus effectivement lus. Vérification manuelle recommandée.</span></div></div>;return<div className={styles.evidenceProof}><Link2 size={14}/><div><b>{evidence.kind==="official"?"Preuve institutionnelle":"Mémoire stratégique du dossier"} — {evidence.source_title}</b><blockquote>{evidence.excerpt}</blockquote>{evidence.source_url&&<a href={evidence.source_url} target="_blank" rel="noreferrer">Ouvrir la source</a>}</div></div>}
function ExpressResult({note}:{note:Note}){return<div className={styles.expressGrid}><article className={styles.expressCard}><div className={styles.expressHead}><AlertTriangle size={18}/><h3>Alertes prioritaires</h3></div>{note.risks?.length?<ol>{note.risks.slice(0,3).map((item,index)=><li key={index}>{item}</li>)}</ol>:<p>Aucune alerte prioritaire identifiée.</p>}</article><article className={styles.expressCard}><div className={styles.expressHead}><Sparkles size={18}/><h3>3 actions à lancer</h3></div>{note.recommendations?.length?<ol>{note.recommendations.slice(0,3).map((item,index)=><li key={index}>{item}</li>)}</ol>:<p>Aucune action prioritaire identifiée.</p>}</article>{note.deadlines?.[0]&&<article className={`${styles.expressCard} ${styles.expressWide}`}><div className={styles.expressHead}><Target size={18}/><h3>Prochaine fenêtre d’action</h3></div><p>{note.deadlines[0]}</p></article>}</div>}
function Provisions({items}:{items?:ImpactDisposition[]}){if(!items?.length)return null;return<div className={styles.provisions}><div className={styles.provisionsHead}><FileText size={18}/><div><h3>Dispositions concernées</h3><span>Chaque disposition est rapprochée d’un passage réellement retrouvé dans les sources lues.</span></div></div><div className={styles.provisionList}>{items.map((item,index)=>{const provisionLevel=String(item.niveau||"moyen").replaceAll("_"," ");const provisionKey=provisionLevel.replaceAll(" ","-") as keyof typeof styles;return<article className={styles.provisionRow} key={`${item.disposition}-${index}`}><div className={styles.provisionTop}><b>{item.disposition||"Disposition à préciser"}</b><span className={`${styles.level} ${styles[provisionKey]||""}`}>{provisionLevel}</span></div><p>{item.impact_client||"Impact client à confirmer."}</p><EvidenceProof evidence={item.evidence}/></article>;})}</div></div>}
function Confirmations({items}:{items?:string[]}){if(!items?.length)return null;return<div className={styles.confirmBox}><div className={styles.confirmHead}><AlertTriangle size={18}/><div><h3>Informations à confirmer</h3><span>Points que Myvor n’a pas pu établir avec suffisamment de certitude.</span></div></div><ul>{items.map((item,index)=><li key={index}>{item}</li>)}</ul></div>}
function ScoreBreakdown({detail,justifications,evidence,compact=false}:{detail?:ScoreDetail|null;justifications?:ScoreJustifications|null;evidence?:ScoreEvidence;compact?:boolean}){if(!detail)return null;return<div className={styles.scoreBreakdown}><div className={styles.scoreBreakdownHead}><div><b>{compact?"Fondements vérifiables du score":"Justification détaillée et preuves de la notation"}</b><span>{compact?"Chaque sous-score est accompagné de la meilleure preuve retrouvée par Myvor.":"Chaque critère est noté indépendamment et rapproché d’une preuve institutionnelle ou, lorsque pertinent, de la mémoire stratégique du dossier."}</span></div></div><div className={styles.scoreMetricGrid}>{scoreLabels.map(metric=><article className={styles.scoreMetric} key={metric.key}><div className={styles.scoreMetricTop}><span>{metric.label}</span><strong>{Math.round(Number(detail[metric.key])||0)}<small>/{metric.max}</small></strong></div><p>{justifications?.[metric.key]?.trim()||"Justification détaillée non disponible pour ce critère."}</p><EvidenceProof evidence={evidence?.[metric.key]}/></article>)}</div></div>}
function ImpactSection({title,icon,items}:{title:string;icon:React.ReactNode;items?:string[]}){return<article className={styles.impactSection}><div className={styles.sectionHead}>{icon}<h3>{title}</h3></div>{items?.length?<ul>{items.map((item,index)=><li key={index}>{item}</li>)}</ul>:<p>Aucun élément identifié.</p>}</article>}
