# watch-jorf-repair

Réparation stricte des anciens JORF : un corps n'est accepté que si le champ XML `<ID>` correspond exactement au `JORFTEXT` de l'URL. Le worker peut aussi restaurer le titre et la nature juridiques depuis l'archive DILA officielle.
