import {createClient} from "npm:@supabase/supabase-js@2.111.0";

type Dossier={id:string;user_id:string;title:string;objective:string;context?:string;watch_keywords?:string[];watch_priority_phrases?:string[];watch_excluded_keywords?:string[];watch_topics?:string[];watch_subtopics?:string[];reference_texts?:string[]};
type WatchItem={id:string;user_id:string;title:string;nature:string;source_url:string;dossier_id:string|null;suggested_dossier_id?:string|null;qualification_reason?:string|null;published_at?:string|null;created_at?:string|null};
type Setting={user_id:string;enabled:boolean;auto_link_threshold:number|string;review_threshold:number|string};
type Score={score:number;matches:string[];primaryPriority:string[];primaryExact:string[];secondarySignals:string[];blockedBy:string|null};
const JSON_HEADERS={"Content-Type":"application/json; charset=utf-8","Cache-Control":"no-store"};
const FUNCTION_PATH="/functions/v1/sync-watch",RULE_PREFIX="Règles dossier v14 —",PROCESS_LIMIT=24,HALF_LIMIT=Math.floor(PROCESS_LIMIT/2);
const STOP_WORDS=new Set(["avec","dans","pour","sans","sous","entre","vers","chez","plus","moins","ainsi","comme","cette","celui","celle","ceux","elles","leurs","notre","votre","nous","vous","tout","tous","toute","toutes","texte","obtenir","modification","favorable","reforme","projet","proposition","objectif","client","dossier","action","impact","enjeu","enjeux","suivi","veille","mesure","mesures","nouveau","nouvelle","relatif","relative","concernant","article","articles","application","applicable","regle","regles","aide","autorisation","transmission","donnee","donnees","collecte","relation"]);
const WEAK_CONFIG_WORDS=new Set(["journal","officiel","assemblee","nationale","senat","gouvernement","constitution","ministere","ministre","administration","public","publique","france","etat","europe","europeen","europeenne","union","commission","conseil","article","articles","loi","lois","decret","decrets","arrete","arretes","code","publication","publie","publiee","actualite","actualites","communique","communiques","adopte","adoptee","adoption","entreprise","entreprises","service","services","reglement","reglements","texte","textes","decision","decisions","avis"]);
const GENERIC_CONFIG_TERMS=new Set(["journal officiel","journal officiel de la republique francaise","assemblee nationale","senat","gouvernement","constitution","de la constitution","conseil d etat","conseil constitutionnel","commission europeenne","union europeenne","adoptee par l assemblee nationale","adopte par l assemblee nationale","publie au journal officiel","publication au journal officiel"]);
const GENERIC_NAV_TITLES=["accueil","particuliers","professionnels","entreprises","associations","vie associative","être informé","etre informe","accéder à la rubrique","acceder a la rubrique","actualités et communiqués","actualites et communiques","actualités","actualites","communiqués","communiques","agenda et événements","agenda et evenements","agenda","événements","evenements","les publications","publications","les prises de parole","prises de parole","newsletter","quels sont mes droits","achats et publicité","achats et publicite","banque assurance","les pratiques numériques des français","les pratiques numeriques des francais","auditions devant le parlement","les auditions devant le parlement"];
function json(body:unknown,status=200){return new Response(JSON.stringify(body),{status,headers:JSON_HEADERS});}
function clip(value:unknown,max:number){return String(value??"").normalize("NFKC").replace(/[\u0000-\u001f\u007f]/g,"").slice(0,max).trim();}
function getAdminKey(){const modern=Deno.env.get("SUPABASE_SECRET_KEYS");if(modern){try{const keys=JSON.parse(modern);const value=keys?.default||Object.values(keys||{})[0];if(typeof value==="string"&&value)return value;}catch{}}return Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")||"";}
function normalize(value:string){return value.normalize("NFD").replace(/[\u0300-\u036f]/g,"").toLowerCase().replace(/[^a-z0-9]+/g," ").replace(/\s+/g," ").trim();}
function isGenericNavigationTitle(value:string){const title=normalize(value);return !title||GENERIC_NAV_TITLES.some(entry=>title===normalize(entry));}
function isStrongConfiguredTerm(value:string){const n=normalize(value);if(!n||n.length<5||GENERIC_CONFIG_TERMS.has(n))return false;if(/\b(?:20\d{2}[- ]\d+|[lrd]\s*\d{2,}|legiarti|jorftext)\b/i.test(n))return true;const domain=n.split(/\s+/).filter(w=>w.length>=5&&!WEAK_CONFIG_WORDS.has(w)&&!/^\d+$/.test(w));return domain.length>=1;}
function instrumentAliases(dossier:Dossier){const title=normalize(dossier.title),aliases:string[]=[];if(/\bia act\b/.test(title)||/\bai act\b/.test(title))aliases.push("ia act","ai act","artificial intelligence act","reglement sur l intelligence artificielle","2024 1689");const datedLaw=title.match(/\bloi du \d{1,2} (?:janvier|fevrier|mars|avril|mai|juin|juillet|aout|septembre|octobre|novembre|decembre) 20\d{2}\b/);if(datedLaw){aliases.push(datedLaw[0]);const tail=title.slice((title.indexOf(datedLaw[0])+datedLaw[0].length)).replace(/^\s*de\s+/,"").split(/\bimpact\b|\bapplication\b|\bconsequences?\b/)[0].trim();const tailWords=tail.split(/\s+/).filter(w=>w.length>=4&&!WEAK_CONFIG_WORDS.has(w));if(tailWords.length>=2&&tail.length>=12)aliases.push(tail);}const numbered=title.match(/\b(?:loi|decret|ordonnance|reglement)(?: ue)?(?: n)? 20\d{2} \d{2,}\b/);if(numbered)aliases.push(numbered[0]);return[...new Set(aliases.map(normalize).filter(Boolean))];}
function stemWord(word:string){const stripped=word.replace(/(issements?|ements?|ations?|itions?|iques?|istes?|ismes?|teurs?|trices?|eurs?|euses?|ites?|ives?|ifs?|aux|ales?|elles?|ments?|es|s)$/i,"");return stripped.length>=4?stripped:word;}
function keywords(value:string){const out:string[]=[];for(const raw of normalize(value).split(/\s+/)){if(raw.length<4||STOP_WORDS.has(raw)||WEAK_CONFIG_WORDS.has(raw)||/^\d+$/.test(raw))continue;const word=stemWord(raw);if(word.length>=4&&!STOP_WORDS.has(word)&&!WEAK_CONFIG_WORDS.has(word))out.push(word);}return[...new Set(out)];}
function cleanedList(value:unknown){return Array.isArray(value)?value.map(v=>String(v||"").trim()).filter(Boolean):[];}
function containsPhrase(normalizedText:string,phrase:string){const needle=normalize(phrase);return !!needle&&` ${normalizedText} `.includes(` ${needle} `);}
function keywordHit(itemWords:Set<string>,word:string){if(itemWords.has(word))return 1;if(word.length<6)return 0;for(const itemWord of itemWords)if(itemWord.length>=6&&(itemWord.startsWith(word)||word.startsWith(itemWord)))return .7;return 0;}
function substantiveScope(title:string,sourceText:string){if(!sourceText)return title;const lower=sourceText.toLowerCase();const starts=["publics concernés","publics concernes","objet :","objet:","objet "].map(marker=>lower.indexOf(marker)).filter(index=>index>=0);let body="";if(starts.length){const start=Math.min(...starts),slice=sourceText.slice(start),sliceLower=slice.toLowerCase();const endMarkers=["le premier ministre","la première ministre","la premiere ministre","le président de la république","le president de la republique","la ministre ","le ministre "];const ends=endMarkers.map(marker=>sliceLower.indexOf(marker,300)).filter(index=>index>300);const end=ends.length?Math.min(...ends):Math.min(slice.length,5000);body=slice.slice(0,end);}else body=sourceText.slice(0,3500);return `${title} ${body}`;}
function scoreItem(primaryText:string,fullText:string,dossier:Dossier):Score{const primary=normalize(primaryText),full=normalize(fullText||primaryText),aliases=instrumentAliases(dossier);if(aliases.length&&!aliases.some(alias=>containsPhrase(full,alias)))return{score:0,matches:[],primaryPriority:[],primaryExact:[],secondarySignals:[],blockedBy:`instrument nommé absent (${aliases.slice(0,2).join(" / ")})`};const primaryWords=new Set(keywords(primary)),fullWords=new Set(keywords(full));const excluded=cleanedList(dossier.watch_excluded_keywords);const watchKeywords=cleanedList([...(dossier.watch_keywords||[]),...(dossier.watch_topics||[]),...(dossier.watch_subtopics||[]),...(dossier.reference_texts||[])]).filter(isStrongConfiguredTerm);const priority=cleanedList([...(dossier.watch_priority_phrases||[]),...(dossier.reference_texts||[])]).filter(isStrongConfiguredTerm);const blockedBy=excluded.find(term=>containsPhrase(full,term))||null;if(blockedBy)return{score:0,matches:[],primaryPriority:[],primaryExact:[],secondarySignals:[],blockedBy};const primaryPriority=priority.filter(term=>containsPhrase(primary,term)),primaryExact=watchKeywords.filter(term=>containsPhrase(primary,term));const fullPriority=priority.filter(term=>containsPhrase(full,term)),fullExact=watchKeywords.filter(term=>containsPhrase(full,term));if(!watchKeywords.length&&!primaryPriority.length&&!fullPriority.length)return{score:0,matches:[],primaryPriority:[],primaryExact:[],secondarySignals:[],blockedBy:null};const primaryMulti=primaryExact.filter(term=>normalize(term).split(/\s+/).length>=2),primarySingles=primaryExact.filter(term=>normalize(term).split(/\s+/).length===1);const fullMulti=fullExact.filter(term=>normalize(term).split(/\s+/).length>=2),fullSingles=fullExact.filter(term=>normalize(term).split(/\s+/).length===1);const words=[...new Set(watchKeywords.flatMap(value=>keywords(value)))],primaryWordHits=words.filter(word=>keywordHit(primaryWords,word)>0),fullWordHits=words.filter(word=>keywordHit(fullWords,word)>0);let score=0;if(primaryPriority.length)score=.99;else if(primaryMulti.length)score=.96;else if(primarySingles.length>=2)score=.86;else if(primarySingles.length===1)score=.62;else if(fullPriority.length)score=.84;else if(fullMulti.length)score=.82;else if(fullSingles.length>=2)score=.78;else if(fullSingles.length===1)score=.60;else if(primaryWordHits.length>=3)score=.74;else if(primaryWordHits.length===2)score=.68;else if(primaryWordHits.length===1)score=.52;else if(fullWordHits.length>=3)score=.66;else if(fullWordHits.length===2)score=.60;else if(fullWordHits.length===1)score=.50;const secondarySignals=[...new Set([...fullPriority.filter(x=>!primaryPriority.includes(x)),...fullExact.filter(x=>!primaryExact.includes(x))])].slice(0,6);const matches=[...new Set([...primaryPriority,...primaryExact,...primaryWordHits,...secondarySignals,...fullWordHits])].slice(0,10);return{score,matches,primaryPriority:primaryPriority.slice(0,4),primaryExact:primaryExact.slice(0,6),secondarySignals,blockedBy:null};}
function itemTime(item:WatchItem){const raw=item.published_at||item.created_at||"";const t=Date.parse(raw);return Number.isFinite(t)?t:0;}
function selectHistoricalAndRecent(items:WatchItem[]){const eligible=items.filter(item=>!item.dossier_id&&!String(item.qualification_reason||"").startsWith(RULE_PREFIX)&&!String(item.qualification_reason||"").startsWith("Ignoré manuellement"));const oldest=[...eligible].sort((a,b)=>itemTime(a)-itemTime(b)).slice(0,HALF_LIMIT);const newest=[...eligible].sort((a,b)=>itemTime(b)-itemTime(a)).slice(0,HALF_LIMIT);const seen=new Set<string>(),merged:WatchItem[]=[];for(const item of [...oldest,...newest]){if(seen.has(item.id))continue;seen.add(item.id);merged.push(item);if(merged.length>=PROCESS_LIMIT)break;}return merged;}
async function verifyInternal(req:Request,supabase:any){const len=Number(req.headers.get("content-length")||0);if(Number.isFinite(len)&&len>4096)return false;const tsRaw=req.headers.get("x-myvor-timestamp")||"",nonce=req.headers.get("x-myvor-nonce")||"",signature=(req.headers.get("x-myvor-signature")||"").toLowerCase();if(!/^\d{10}$/.test(tsRaw)||!/^[0-9a-f-]{36}$/i.test(nonce)||!/^[0-9a-f]{64}$/.test(signature))return false;const ts=Number(tsRaw);if(Math.abs(Math.floor(Date.now()/1000)-ts)>120)return false;const{data,error}=await supabase.rpc("verify_veille_internal_request",{p_path:FUNCTION_PATH,p_timestamp:ts,p_nonce:nonce,p_signature:signature});return !error&&data===true;}
Deno.serve(async req=>{if(req.method!=="POST")return json({error:"Méthode non autorisée"},405);const supabaseUrl=Deno.env.get("SUPABASE_URL")||"",adminKey=getAdminKey();if(!supabaseUrl||!adminKey)return json({error:"Configuration Supabase serveur incomplète"},503);const supabase=createClient(supabaseUrl,adminKey,{auth:{persistSession:false,autoRefreshToken:false}});if(!await verifyInternal(req,supabase))return json({error:"Non autorisé"},401);const{data:settings,error:settingsError}=await supabase.from("veille_settings").select("user_id,enabled,auto_link_threshold,review_threshold").eq("enabled",true);if(settingsError)return json({ok:false,error:"Impossible de charger les réglages de veille"},500);const summaries:any[]=[];for(const setting of(settings||[]) as Setting[]){const userId=setting.user_id,engine="catalog-history-dossier-relevance-v14";const{data:run,error:runError}=await supabase.from("veille_runs").insert({user_id:userId,status:"running",sources_count:1,fetched_count:0,engine}).select("id").single();if(runError){summaries.push({status:runError.code==="23505"?"skipped":"error",message:runError.code==="23505"?"already_running":"run_log_failed"});continue;}const runId=run.id as string;try{const[{data:dossiers,error:dossierError},{data:items,error:itemError}]=await Promise.all([supabase.from("dossiers").select("id,user_id,title,objective,context,watch_keywords,watch_priority_phrases,watch_excluded_keywords,watch_topics,watch_subtopics,reference_texts").eq("user_id",userId),supabase.from("watch_items").select("id,user_id,title,nature,source_url,dossier_id,suggested_dossier_id,qualification_reason,published_at,created_at").eq("user_id",userId)]);if(dossierError||itemError)throw new Error("Lecture du portefeuille impossible");const allItems=(items||[]) as WatchItem[],allDossiers=(dossiers||[]) as Dossier[];const processItems=selectHistoricalAndRecent(allItems);const ids=processItems.map(item=>item.id);let contentRows:any[]=[];if(ids.length){const{data,error}=await supabase.from("watch_item_content").select("watch_item_id,source_text,source_text_chars").in("watch_item_id",ids);if(error)throw new Error(`Lecture contenu source impossible: ${clip(error.message,180)}`);contentRows=data||[];}const contentById=new Map(contentRows.map((row:any)=>[String(row.watch_item_id),row]));const reviewThreshold=Math.max(.60,Math.min(1,Number(setting.review_threshold)||.60));let candidates=0,rejected=0,withContent=0,totalChars=0,oldestProcessed:string|null=null,newestProcessed:string|null=null;for(const item of processItems){const t=itemTime(item),iso=t?new Date(t).toISOString():null;if(iso){if(!oldestProcessed||iso<oldestProcessed)oldestProcessed=iso;if(!newestProcessed||iso>newestProcessed)newestProcessed=iso;}const cached:any=contentById.get(item.id),sourceText=String(cached?.source_text||"");if(sourceText){withContent++;totalChars+=Number(cached?.source_text_chars)||sourceText.length;}if(isGenericNavigationTitle(item.title)){await supabase.from("watch_items").update({dossier_id:null,suggested_dossier_id:null,qualification_confidence:0,qualification_reason:`${RULE_PREFIX} Page générique détectée : aucun rattachement.`,qualified_at:new Date().toISOString(),urgency:"faible"}).eq("id",item.id).eq("user_id",userId);rejected++;continue;}const primary=substantiveScope(item.title,sourceText),full=`${item.title} ${item.nature} ${sourceText.slice(0,12000)}`;const ranked=allDossiers.map(d=>({d,...scoreItem(primary,full,d)})).sort((a,b)=>b.score-a.score),best=ranked[0],second=ranked[1];if(!best||best.score<reviewThreshold){const blocked=ranked.find(x=>x.blockedBy);const reason=blocked?`${RULE_PREFIX} Exclusion détectée : ${blocked.blockedBy}.`:`${RULE_PREFIX} Aucun signal métier suffisamment discriminant dans le titre ou le contenu juridique.`;await supabase.from("watch_items").update({dossier_id:null,suggested_dossier_id:null,qualification_confidence:0,qualification_reason:reason,qualified_at:new Date().toISOString(),urgency:"faible"}).eq("id",item.id).eq("user_id",userId);rejected++;continue;}let confidence=best.score;if(second&&second.score>=reviewThreshold&&(best.score-second.score)<.12)confidence=Math.min(confidence,.84);const signals=[best.primaryPriority.length?`Expression prioritaire dans le sujet : ${best.primaryPriority.join(", ")}.`:"",best.primaryExact.length?`Mots-clés exacts dans le sujet : ${best.primaryExact.join(", ")}.`:"",best.secondarySignals.length?`Signaux secondaires dans le texte : ${best.secondarySignals.join(", ")}.`:"",!best.primaryPriority.length&&!best.primaryExact.length&&best.matches.length?`Signaux lexicaux : ${best.matches.join(", ")}.`:""].filter(Boolean).join(" ");const reason=`${RULE_PREFIX} ${signals} Validation IA en attente.`;const{error:updateError}=await supabase.from("watch_items").update({dossier_id:null,suggested_dossier_id:best.d.id,qualification_confidence:Number(confidence.toFixed(2)),qualification_reason:clip(reason,700),qualified_at:new Date().toISOString(),urgency:"moyen"}).eq("id",item.id).eq("user_id",userId);if(updateError)throw new Error(`Mise à jour qualification impossible: ${clip(updateError.message,180)}`);candidates++;}const remaining=Math.max(0,allItems.filter(item=>!item.dossier_id&&!String(item.qualification_reason||"").startsWith(RULE_PREFIX)&&!String(item.qualification_reason||"").startsWith("Ignoré manuellement")).length-processItems.length);const message=`${processItems.length} texte(s) rescanné(s), sans limite d'ancienneté · ${withContent} avec contenu source · ${totalChars} caractères · ${candidates} candidat(s) IA · ${rejected} écarté(s) · ${remaining} restant(s) à rescanner.`;await supabase.from("veille_runs").update({status:"success",finished_at:new Date().toISOString(),fetched_count:processItems.length,new_count:0,auto_linked_count:0,review_count:candidates,actions_created_count:0,engine,message}).eq("id",runId);await supabase.from("veille_settings").update({last_run_at:new Date().toISOString(),updated_at:new Date().toISOString()}).eq("user_id",userId);summaries.push({status:"success",processed:processItems.length,with_content:withContent,candidates,rejected,remaining,oldest_processed:oldestProcessed,newest_processed:newestProcessed,engine});}catch(error:any){const message=clip(error?.message||"Erreur inconnue",500);await supabase.from("veille_runs").update({status:"error",finished_at:new Date().toISOString(),message}).eq("id",runId);summaries.push({status:"error",message});}}return json({ok:true,synced_at:new Date().toISOString(),source:"catalog-history",users:summaries});});