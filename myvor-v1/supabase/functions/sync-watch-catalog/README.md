# sync-watch-catalog

Catalogue JORF exclusivement fondé sur les archives officielles DILA `JORFSIMPLE`. Le XML n'est accepté que si son propre `<ID>` est un `JORFTEXT`; les conteneurs `JORFCONT` ne peuvent plus être enregistrés comme corps d'un texte.
